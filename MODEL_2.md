# 김은호 풍력발전량 예측 모델 2

이 문서는 `src/model_2.py`가 어떤 순서로 발전량을 예측하는지 설명합니다. 모델 코드는 기존 예측 CSV를 가져와 조립하지 않고, 저장소의 `wind_data`에서 시작해 전처리·검증·전체 재학습·제출 파일 생성을 한 번에 수행합니다.

## 실행 방법

저장소 루트에서 다음 명령을 실행합니다.

```powershell
python src/model_2.py
```

필요한 주요 패키지는 `numpy`, `pandas`, `scikit-learn`, `lightgbm`, `catboost`, `xgboost`, `matplotlib`, `seaborn`, `joblib`, `IPython`입니다.

최종 제출 파일은 다음 경로에 생성됩니다.

```text
model_2_output/submission_model_2.csv
```

전체 모델을 다시 학습하므로 CPU와 메모리 성능에 따라 실행에 수 시간이 걸릴 수 있습니다.

## 전체 흐름

```text
wind_data 원본
   ↓
기존 전처리와 장기정지 행 복원
   ↓
LightGBM·CatBoost 기준 앙상블
   ↓
통합 후보와 그룹별 전문가 학습
   ↓
Group 1: 최근연도·고출력 가중 앙상블
Group 2: Gaussian Mixture of Experts
Group 3: Pooled 앙상블과 제한형 고출력 Gate
   ↓
검증을 통과한 구조만 적용
   ↓
2022~2024년 전체 라벨 재학습
   ↓
제출 CSV 생성
```

## 데이터와 Target

모델은 GFS·LDAPS 기상 예보, 시간 피처, 풍속 변화량, 이동 통계와 풍속 세제곱 등의 파생 피처를 사용합니다.

그룹마다 설비용량이 다르므로 발전량을 설비용량으로 나눈 Capacity Factor(CF)를 예측합니다.

```text
CF = 실제 발전량 / 그룹 설비용량
최종 발전량 = 예측 CF × 그룹 설비용량
```

설비용량은 Group 1과 Group 2가 21,600kWh, Group 3이 21,000kWh입니다. 공식 평가 대상에 맞춰 실제 CF 0.10 이상인 활성 발전 구간을 중심으로 모델을 학습하고 검증합니다.

SCADA에서 확인한 장기정지 행은 삭제하지 않고 복원합니다. 기준 모델 일부에는 장기정지 행의 Sample Weight 0.5를 적용하지만, 미래 Test에서 실제 장기정지 여부를 알 수 없으므로 장기정지 플래그 자체는 Test 입력 피처로 사용하지 않습니다.

## 검증과 전체 재학습

기본 모델은 2022~2023년 데이터로 학습하고 2024년을 외부 검증으로 사용합니다. 후속 앙상블과 Gate·MoE 파라미터는 2024년 내부의 선택 구간과 확인 구간에서 함께 비교합니다.

한 구간에서만 좋아진 후보가 최종 적용되지 않도록 NMAE 악화 제한, 기간별 점수와 FICR 조건을 확인합니다. 조건을 통과하지 못한 보정은 적용하지 않고 기존 기본 모델로 돌아갑니다.

모델 구조를 선택한 뒤에는 2024년을 버리지 않고 2022~2024년 전체 라벨을 사용해 최종 모델을 다시 학습합니다. Random Seed는 42로 고정합니다.

## Group 1

Group 1은 평균 회귀가 낮은 출력을 높게, 높은 출력을 낮게 예측하는 수축 현상을 줄이기 위해 Quantile 모델과 고출력 가중 전문가를 함께 사용합니다.

기본 Anchor는 다음과 같습니다.

```text
CatBoost Quantile 0.55 × 40%
+ LightGBM Quantile 0.65 × 60%
```

Anchor에 고출력 가중 LightGBM을 추가한 통합 모델은 다음과 같습니다.

```text
기본 Anchor × 70%
+ 고출력 가중 LightGBM × 30%
```

최종 Group 1 예측은 다음과 같습니다.

```text
통합 모델 × 75%
+ 최근연도 가중 LightGBM × 25%
```

최근연도 전문가는 2024년 학습 행에 1.5배의 가중치를 적용합니다. 출력별 Sample Weight는 CF 0.50 미만 1.0, 0.50~0.70 구간 1.2, 0.70~0.90 구간 2.0, 0.90 이상 6.0입니다.

검증에서 확인 구간을 통과하지 못한 별도 잔차 보정과 분산 보정은 최종 Group 1에 적용하지 않습니다.

## Group 2 Gaussian MoE

Group 2는 하나의 모델로 모든 출력 구간을 처리하지 않고 CF 10~30%, 30~60%, 60~80%, 80~100% 구간을 중심으로 네 전문가를 학습합니다. 각 전문가는 해당 범위의 행만 단순히 잘라서 학습하는 것이 아니라, 담당 범위 주변 행에 Gaussian Sample Weight를 더 크게 적용합니다.

기본 예측 CF와 각 Gaussian 중심의 거리가 가까울수록 해당 전문가의 예측 비중이 커집니다. 분포가 서로 겹치므로 경계에서는 여러 전문가가 동시에 반영됩니다.

| 전문가 | 중심 | 표준편차 | 진폭 |
|---|---:|---:|---:|
| CF 10~30% | 0.188690 | 0.082836 | 0.590295 |
| CF 30~60% | 0.370650 | 0.105546 | 1.084542 |
| CF 60~80% | 0.623235 | 0.152329 | 0.681237 |
| CF 80~100% | 0.873662 | 0.126600 | 0.889503 |

전문가들이 서로 다른 예측을 내는 행은 출력 구간이 불확실할 가능성이 있으므로 보정 강도를 자동으로 낮춥니다. `base_alpha`는 0.545151, 불일치 감쇠값 `damping`은 3.0, 한 행의 최대 보정량 `cap`은 0.07 CF입니다.

Gaussian MoE는 Group 2에만 적용합니다. 전문가 예측으로 기본 예측을 완전히 교체하지 않고, 전문가들이 비교적 일치하는 경우에만 제한적으로 전문가 혼합값 방향으로 이동합니다.

## Group 3

Group 3은 학습 기간이 짧아 그룹 전용 모델만으로는 표본이 부족할 수 있습니다. 따라서 세 그룹을 함께 학습한 Pooled CatBoost 90%와 Group 3 전용 CatBoost Quantile 0.55 모델 10%를 기본 예측으로 사용합니다.

고출력 보정에는 두 모델을 추가합니다.

- 실제 CF 0.80 이상 여부를 예측하는 LightGBM 분류기
- 실제 CF 0.70 이상인 행을 중심으로 학습한 Quantile 0.65 고출력 전문가

다음 조건을 모두 만족할 때만 고출력 전문가 방향으로 상향합니다.

```text
고출력 확률 ≥ 0.25
기본 예측 CF ≥ 0.65
고출력 전문가 예측 > 기본 예측
```

보정 반영률 `gamma`는 0.30이고 최대 상향량은 0.10 CF입니다. Gate와 고출력 전문가를 학습할 때 2024년 행에는 2.0의 최근연도 가중치를 적용합니다.

Group 3의 구조는 Gaussian MoE가 아닙니다. 확률과 기본 예측 범위를 모두 확인한 뒤 일부 행만 수정하는 제한형 고출력 Gate입니다.

## 생성되는 주요 폴더

- `wind_baram_v2_preprocessed`: 복원 전처리 데이터
- `wind_baram_v2_output`: 기준 앙상블 검증 및 예측
- `wind_hybrid_2stage_output`: 통합 후보 검증 및 예측
- `wind_optimal_group_selector_output`: Group 1·3 선택 결과
- `wind_g2_gaussian_moe_output`: Group 2 Gaussian MoE 결과
- `model_2_output`: 최종 제출과 선택 설정

이 폴더들은 실행 결과이므로 Git에 커밋하지 않습니다.

## 재현 시 주의사항

- 모델은 저장소 루트의 `wind_data` 구조를 자동으로 사용합니다.
- 개인 PC의 절대경로나 기존 실험 결과 파일을 요구하지 않습니다.
- 선택되지 않은 보정이나 가중치를 임의로 추가하지 않습니다.
- 패키지 버전과 CPU 병렬 연산 순서에 따라 예측의 마지막 소수점은 달라질 수 있습니다.
- 원본 대회 데이터의 외부 공개 가능 여부는 대회 약관을 확인해야 합니다.
